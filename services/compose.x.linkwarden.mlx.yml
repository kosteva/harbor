services:
  linkwarden:
    depends_on:
      - mlx
    environment:
      - CUSTOM_OPENAI_BASE_URL=http://mlx:8080/v1
      - CUSTOM_OPENAI_NAME=mlx
      - OPENAI_API_KEY=sk-mlx
      - OPENAI_MODEL=${HARBOR_MLX_MODEL}
