# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  dbhub:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dbhub.rule=Host(`dbhub.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.dbhub.loadbalancer.server.port=8080"
      - "traefik.http.routers.dbhub.entrypoints=websecure"
      - "traefik.http.routers.dbhub.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver (verified empirically against a live Traefik
      # v3 instance) — each router needs its own explicit certresolver
      # label or ACME never activates for it.
      - "traefik.http.routers.dbhub.tls.certresolver=letsencrypt"
      - "traefik.http.routers.dbhub.service=dbhub"

    networks:
      - traefik-public
