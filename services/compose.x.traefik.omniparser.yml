# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  omniparser:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.omniparser.rule=Host(`omniparser.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.omniparser.loadbalancer.server.port=7861"
      - "traefik.http.routers.omniparser.entrypoints=websecure"
      - "traefik.http.routers.omniparser.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.omniparser.tls.certresolver=letsencrypt"
      - "traefik.http.routers.omniparser.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.omniparser.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.omniparser.service=omniparser"

    networks:
      - traefik-public
