# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  openfang:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.openfang.rule=Host(`openfang.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.openfang.loadbalancer.server.port=4200"
      - "traefik.http.routers.openfang.entrypoints=websecure"
      - "traefik.http.routers.openfang.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.openfang.tls.certresolver=letsencrypt"
      - "traefik.http.routers.openfang.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.openfang.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.openfang.service=openfang"

    networks:
      - traefik-public
