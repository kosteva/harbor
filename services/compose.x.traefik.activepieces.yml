# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  activepieces:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.activepieces.rule=Host(`activepieces.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.activepieces.loadbalancer.server.port=80"
      - "traefik.http.routers.activepieces.entrypoints=websecure"
      - "traefik.http.routers.activepieces.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.activepieces.tls.certresolver=letsencrypt"
      - "traefik.http.routers.activepieces.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.activepieces.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.activepieces.service=activepieces"

    networks:
      - traefik-public
