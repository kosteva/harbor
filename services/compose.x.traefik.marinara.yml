# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  marinara:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.marinara.rule=Host(`marinara.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.marinara.loadbalancer.server.port=7860"
      - "traefik.http.routers.marinara.entrypoints=websecure"
      - "traefik.http.routers.marinara.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.marinara.tls.certresolver=letsencrypt"
      - "traefik.http.routers.marinara.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.marinara.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.marinara.service=marinara"

    networks:
      - traefik-public
