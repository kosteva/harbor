# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  omnichain:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.omnichain.rule=Host(`omnichain.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.omnichain.loadbalancer.server.port=${HARBOR_OMNICHAIN_HOST_PORT}"
      - "traefik.http.routers.omnichain.entrypoints=websecure"
      - "traefik.http.routers.omnichain.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver (verified empirically against a live Traefik
      # v3 instance) — each router needs its own explicit certresolver
      # label or ACME never activates for it. No tls.domains label: this
      # gets each service its own individual certificate, auto-derived
      # from its own Host() rule (Traefik's default behavior).
      - "traefik.http.routers.omnichain.tls.certresolver=letsencrypt"
      - "traefik.http.routers.omnichain.service=omnichain"

    networks:
      - traefik-public
