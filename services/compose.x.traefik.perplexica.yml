# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  perplexica:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.perplexica.rule=Host(`perplexica.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.perplexica.loadbalancer.server.port=3000"
      - "traefik.http.routers.perplexica.entrypoints=websecure"
      - "traefik.http.routers.perplexica.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.perplexica.tls.certresolver=letsencrypt"
      - "traefik.http.routers.perplexica.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.perplexica.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.perplexica.service=perplexica"

    networks:
      - traefik-public
