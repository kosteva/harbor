# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  daytona:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.daytona.rule=Host(`daytona.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.daytona.loadbalancer.server.port=3000"
      - "traefik.http.routers.daytona.entrypoints=websecure"
      - "traefik.http.routers.daytona.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.daytona.tls.certresolver=letsencrypt"
      - "traefik.http.routers.daytona.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.daytona.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.daytona.service=daytona"

    networks:
      - traefik-public
