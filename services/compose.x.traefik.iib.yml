# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  iib:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.iib.rule=Host(`iib.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.iib.loadbalancer.server.port=8080"
      - "traefik.http.routers.iib.entrypoints=websecure"
      - "traefik.http.routers.iib.tls=true"
      - "traefik.http.routers.iib.service=iib"

    networks:
      - traefik-public
