# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  tei:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tei.rule=Host(`tei.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.tei.loadbalancer.server.port=80"
      - "traefik.http.routers.tei.entrypoints=websecure"
      - "traefik.http.routers.tei.tls=true"
      - "traefik.http.routers.tei.service=tei"

    networks:
      - traefik-public
