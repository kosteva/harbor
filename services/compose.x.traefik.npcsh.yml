# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  npcsh:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.npcsh.rule=Host(`npcsh.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.npcsh.loadbalancer.server.port=5337"
      - "traefik.http.routers.npcsh.entrypoints=websecure"
      - "traefik.http.routers.npcsh.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.npcsh.tls.certresolver=letsencrypt"
      - "traefik.http.routers.npcsh.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.npcsh.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.npcsh.service=npcsh"

    networks:
      - traefik-public
