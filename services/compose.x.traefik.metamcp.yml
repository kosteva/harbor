# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  metamcp:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.metamcp.rule=Host(`metamcp.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.metamcp.loadbalancer.server.port=3000"
      - "traefik.http.routers.metamcp.entrypoints=websecure"
      - "traefik.http.routers.metamcp.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.metamcp.tls.certresolver=letsencrypt"
      - "traefik.http.routers.metamcp.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.metamcp.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.metamcp.service=metamcp"

    networks:
      - traefik-public
