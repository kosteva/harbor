services:
  marinara:
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      # Container-internal addresses are private IPs; Marinara blocks those unless
      # the local-URL gate is open. Create the connection in the UI with
      # Base URL http://ollama:11434/v1
      - PROVIDER_LOCAL_URLS_ENABLED=true
