# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  mcpforge:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mcpforge.rule=Host(`mcpforge.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.mcpforge.loadbalancer.server.port=4444"
      - "traefik.http.routers.mcpforge.entrypoints=websecure"
      - "traefik.http.routers.mcpforge.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.mcpforge.tls.certresolver=letsencrypt"
      - "traefik.http.routers.mcpforge.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.mcpforge.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.mcpforge.service=mcpforge"

    networks:
      - traefik-public
