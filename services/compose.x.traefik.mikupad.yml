# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  mikupad:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mikupad.rule=Host(`mikupad.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.mikupad.loadbalancer.server.port=8080"
      - "traefik.http.routers.mikupad.entrypoints=websecure"
      - "traefik.http.routers.mikupad.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.mikupad.tls.certresolver=letsencrypt"
      - "traefik.http.routers.mikupad.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.mikupad.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.mikupad.service=mikupad"

    networks:
      - traefik-public
