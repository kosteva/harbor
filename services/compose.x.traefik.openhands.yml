# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  openhands:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.openhands.rule=Host(`openhands.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.openhands.loadbalancer.server.port=3000"
      - "traefik.http.routers.openhands.entrypoints=websecure"
      - "traefik.http.routers.openhands.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.openhands.tls.certresolver=letsencrypt"
      - "traefik.http.routers.openhands.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.openhands.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.openhands.service=openhands"

    networks:
      - traefik-public
