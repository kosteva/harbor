services:
  whishper:
    image: ${HARBOR_WHISHPER_IMAGE}:${HARBOR_WHISHPER_VERSION}-gpu
    environment:
      - PUBLIC_WHISHPER_PROFILE=gpu
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              count: all
              driver: nvidia
