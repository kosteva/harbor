# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  anythingllm:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.anythingllm.rule=Host(`anythingllm.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.anythingllm.loadbalancer.server.port=3001"
      - "traefik.http.routers.anythingllm.entrypoints=websecure"
      - "traefik.http.routers.anythingllm.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.anythingllm.tls.certresolver=letsencrypt"
      - "traefik.http.routers.anythingllm.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.anythingllm.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.anythingllm.service=anythingllm"

    networks:
      - traefik-public
