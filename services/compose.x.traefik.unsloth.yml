# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  unsloth:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.unsloth.rule=Host(`unsloth.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.unsloth.loadbalancer.server.port=8888"
      - "traefik.http.routers.unsloth.entrypoints=websecure"
      - "traefik.http.routers.unsloth.tls=true"
      # A router's tls=true does NOT inherit the websecure entrypoint's
      # default certResolver or domains (verified empirically against a
      # live Traefik v3 instance) — once a router has any explicit tls.*
      # field it stops inheriting entrypoint-level TLS defaults entirely.
      # Without the domains labels below, each router would request its
      # own individual cert (auto-derived from its own Host() rule)
      # instead of reusing the shared wildcard.
      - "traefik.http.routers.unsloth.tls.certresolver=letsencrypt"
      - "traefik.http.routers.unsloth.tls.domains[0].main=${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.unsloth.tls.domains[0].sans=*.${HARBOR_TRAEFIK_DOMAIN}"
      - "traefik.http.routers.unsloth.service=unsloth"

    networks:
      - traefik-public
